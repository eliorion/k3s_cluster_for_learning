apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: telegram
  namespace: flux-system
spec:
  type: telegram
  # Telegram chat id (numeric string). DM = positive, group = negative.
  channel: "-5295319950"
  secretRef:
    name: telegram-bot-token
