apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Grafana datasource (read-only postgres → fbref-db) + dashboard, loaded by the
# kube-prometheus-stack grafana sidecars (monitoring namespace).
resources:
  - datasource.enc.yaml
  - dashboard.yaml
