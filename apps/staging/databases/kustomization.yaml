apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# CNPG clusters and their secrets/backups. db-migrations/ is intentionally
# NOT listed — it is applied by its own Flux Kustomization (db-migrations).
resources:
  - asp/
