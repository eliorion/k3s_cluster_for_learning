apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Reached ONLY via the `fbref-db-migrations` Flux Kustomization
# (clusters/staging/fbref.yaml) — deliberately NOT listed in the parent
# db-migrations/kustomization.yaml, which belongs to the asp pipeline.
namespace: fbref

resources:
  - fbref-db-migrations-job.yaml
