apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fbref

resources:
  - ../../../base/databases/fbref
  # grafana_ro role password (basic-auth) — consumed by the CNPG managed role.
  - fbref-grafana-ro.enc.yaml
  # TODO: UNCOMMENT once ghcr-pull-secret.enc.yaml is added (create the PAT,
  # sops-encrypt it; same name `ghcr-pull-secret`, fbref namespace). Until then
  # fbref pods ImagePullBackOff (also pending the renamed image release).
  # - ghcr-pull-secret.enc.yaml
