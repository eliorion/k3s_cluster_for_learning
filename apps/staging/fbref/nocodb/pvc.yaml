apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nocodb-data
spec:
  accessModes:
    - ReadWriteOnce
  # local-path (k3s default): no expansion — see CLAUDE.md storage notes.
  resources:
    requests:
      storage: 2Gi
