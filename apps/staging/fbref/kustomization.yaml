apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# NOTE: no `namespace:` transformer here — release.yaml is a flux-system
# HelmRelease; nocodb/ sets its own `namespace: fbref` internally.
resources:
  - release.yaml
  - nocodb/
