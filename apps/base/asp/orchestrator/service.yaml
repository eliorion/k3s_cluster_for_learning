apiVersion: v1
kind: Service
metadata:
  name: orchestrator
  namespace: asp
spec:
  type: ClusterIP
  selector:
    app: orchestrator
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: ui
      port: 8501
      targetPort: 8501
