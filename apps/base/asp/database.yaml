apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: asp-db
spec:
  instances: 3
  bootstrap:
    initdb:
      database: automarket
      owner: asp-adb
      secret:
        name: asp-db-credentials
  storage:
    size: 1Gi
