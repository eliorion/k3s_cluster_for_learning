apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# fbref runs in its OWN `fbref` namespace with its own CNPG cluster (fbref-db) —
# fully separate from asp.
resources:
  - namespace.yaml
  - database.yaml
