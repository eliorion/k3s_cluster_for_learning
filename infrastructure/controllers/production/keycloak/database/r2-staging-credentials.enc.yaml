# PLACEHOLDER — NOT ENCRYPTED YET. TEMPORARY (remove after seeding, plan Step 6).
# 1. Replace values with the R2 token that can READ bucket asp-cnpg-staging
#    (reuse the staging r2-backup-credentials token, or a read-scoped one).
# 2. Encrypt in place:  sops -e -i <this file>
apiVersion: v1
kind: Secret
metadata:
    name: r2-staging-credentials
type: Opaque
stringData:
    ACCESS_KEY_ID: REPLACE_WITH_STAGING_R2_ACCESS_KEY_ID
    ACCESS_KEY_SECRET: REPLACE_WITH_STAGING_R2_ACCESS_KEY_SECRET
