# PLACEHOLDER — NOT ENCRYPTED YET.
# 1. Replace values with the scoped R2 token for bucket asp-cnpg-production
#    (Object Read & Write, that bucket only).
# 2. Encrypt in place:  sops -e -i <this file>
apiVersion: v1
kind: Secret
metadata:
  name: r2-backup-credentials
type: Opaque
stringData:
  ACCESS_KEY_ID: "REPLACE_ME"
  ACCESS_KEY_SECRET: "REPLACE_ME"
