apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: identity
#resources:
#  - ../../base/keycloak/
#  - app/
#  - database/r2-backup-credentials.enc.yaml
#  - database/objectstore.yaml
#  - database/scheduledbackup.yaml
#  # TEMPORARY seed-from-staging — remove after seeding (plan Step 6)
#  - database/r2-staging-credentials.enc.yaml
#  - database/objectstore-staging.yaml
patches:
  - path: database/cluster-backup-patch.yaml
    target:
      kind: Cluster
      name: keycloak-db
  # TEMPORARY seed-from-staging — remove after seeding (plan Step 6)
  - path: database/cluster-recovery-patch.yaml
    target:
      kind: Cluster
      name: keycloak-db
